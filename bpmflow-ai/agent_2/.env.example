# Agent 2 standalone env — SUPERSEDED
# Use bpmflow-ai/backend/.env.example instead (one file for all four agents).
# Kept only so older scripts that look here fail closed with a clear pointer.

GEMINI_OFFLINE=true
EMAIL_DRY_RUN=true
ENVIRONMENT=development
