# Supabase Configuration
VITE_SUPABASE_URL=https://qgntndundyjutzkuledp.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# API Configuration
# Local Vite: leave empty (or omit) so the browser calls same-origin /api,
# which vite.config.ts proxies to http://127.0.0.1:8000 — avoids CORS issues.
# Set an absolute URL when the UI is not served by the Vite proxy.
VITE_API_URL=

